subscription Home($where: assets_bool_exp, $orderBy: [assets_order_by!]) {
  assets(where: $where, order_by: $orderBy) {
    ...Assets
  }
}

fragment Assets on assets {
  chainId
  contractAddress
  tokenId
  holder
  metadata
  orders {
    price
  }
}

subscription Asset($where: assets_bool_exp) {
  assets(where: $where) {
    ...Asset
  }
}

fragment Asset on assets {
  chainId
  contractAddress
  tokenId
  holder
  metadata
  orders {
    id
    direction
    protocol
    offerer
    price
    signedOrder
  }
}
