table:
  schema: public
  name: orders_is_valid
object_relationships:
  - name: asset
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: assets
        insertion_order: null
        column_mapping:
          contractAddress: contractAddress
          chainId: chainId
          tokenId: tokenId
