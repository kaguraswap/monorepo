table:
  schema: public
  name: order
object_relationships:
  - name: nft
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: nft
        insertion_order: null
        column_mapping:
          contractAddress: contractAddress
          chainId: chainId
          tokenId: tokenId
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - chainId
        - contractAddress
        - tokenId
        - protocol
        - isValid
        - signedOrder
        - offerer
        - sortablePrice
        - price
        - direction
      filter: {}
