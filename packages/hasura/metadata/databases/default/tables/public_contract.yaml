table:
  schema: public
  name: contract
array_relationships:
  - name: nfts
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: nft
        insertion_order: null
        column_mapping:
          contractAddress: contractAddress
          chainId: chainId
select_permissions:
  - role: anonymous
    permission:
      columns:
        - supportsInterface
        - chainId
        - contractAddress
        - tokenURI
      filter: {}
