table:
  schema: public
  name: contract
array_relationships:
  - name: assets
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: assets
        insertion_order: null
        column_mapping:
          contractAddress: contractAddress
          chainId: chainId
select_permissions:
  - role: anonymous
    permission:
      columns:
        - supportsInterface
        - chainId
        - contractAddress
        - tokenURI
      filter: {}
