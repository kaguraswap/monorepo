table:
  schema: public
  name: orderProtocol
is_enum: true
array_relationships:
  - name: orders
    using:
      foreign_key_constraint_on:
        column: protocol
        table:
          schema: public
          name: orders
